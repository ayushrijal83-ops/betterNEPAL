Flask==3.1.3
Flask-Cors==6.0.5
python-dotenv==1.1.1

# database
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
SQLAlchemy==2.0.54
psycopg2-binary==2.9.13
GeoAlchemy2==0.20.0

# authentication
PyJWT==2.14.0
argon2-cffi==25.1.0

# ai
# NOTE: google-generativeai is deprecated upstream (the repo is now named
# deprecated-generative-ai-python) in favour of the google-genai SDK. Pinned
# here because it is what this phase was specified against; app/services/
# ai_service.py isolates it behind BaseAIService so replacing it is one class.
google-generativeai==0.8.6

# Self-hosted Ollama, reached through its OpenAI-compatible /v1 endpoint.
# The `openai` package here is a protocol client, not a commitment to OpenAI:
# it speaks the same wire format Ollama serves, so no traffic leaves the
# network unless a hosted base_url is configured.
openai==3.16.1

# security
Flask-Limiter==4.1.1
redis==5.2.1

# real-time
Flask-SocketIO==5.5.1

# dev / test
pytest==8.4.2
